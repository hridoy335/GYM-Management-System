@model IEnumerable<GYM_Management_System.Models.ProductPlan>

@{
    ViewBag.Title = "ProductPlan Information";
    Layout = "~/Views/Shared/_MyLayoutPage.cshtml";
}

<h2>ProductPlanInfo</h2>

<p>
    @Html.ActionLink("Create New", "ProductPlan")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.ActionLink("Update", "UpdateProductPlan", new { id=item.ProductPlanId }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ProductPlanId }) |*@
            @Html.ActionLink("Delete", "DeleteProductPlan", new { id=item.ProductPlanId })
        </td>
    </tr>
}

</table>
