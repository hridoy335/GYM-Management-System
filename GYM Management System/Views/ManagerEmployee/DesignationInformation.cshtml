@model IEnumerable<GYM_Management_System.Models.Designation>

@{
    ViewBag.Title = "DesignationInformation";
    Layout = "~/Views/Shared/_Manager_Layout.cshtml";
}

<h2>Designation Information</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesignationName)
        </th>
        @*<th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesignationName)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesignationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesignationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesignationId })
        </td>*@
    </tr>
}

</table>
